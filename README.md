`Report.qmd` depends on `R/module.R`, but not directly on `R/heavy_analysis.R`

`dev/install_dependencies.R` is the script that would be run by GitHub actions workflow to install R package dependencies.
